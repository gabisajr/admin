function MyCtrl($scope) {
  $scope.action = function() {


    $scope.name = 'OK';

    //ajax ,  return data 
    // if data true, forword to the special url
    // else in the current page
  }

  $scope.name = 'World';
}